# python-app/catalog/component.yaml  (archivo nuevo)
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.app_name }}
  description: Python app that display time.
  tags: # Filtros
    - nodejs
    - typescript
    - express
  annotations:
    github.com/project-slug: juancrespo25/${{ values.app_name }}
    backstage.io/techdocs-ref: dir:.. # Docs en raíz.
spec:
  type: service
  owner: development
  lifecycle: experimental